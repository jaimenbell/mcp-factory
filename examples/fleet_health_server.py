#!/usr/bin/env python3
"""Placeholder fleet-health MCP server referenced by fleet_health.yaml.

This stub exists so the example manifest references a real file and the factory
takes the "existing script" path (emitting a config entry rather than scaffolding
a new stub). Replace it with your own server implementation for real use.
"""
from __future__ import annotations


def main() -> None:  # pragma: no cover - example placeholder, not a real server
    raise SystemExit("placeholder: replace with a real MCP server implementation")


if __name__ == "__main__":  # pragma: no cover
    main()
